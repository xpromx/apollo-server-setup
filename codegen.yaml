schema:
  - "./src/**/*.(gql|graphql)"
generates:
  ./src/graphql/schema-compiled.graphql:
    plugins:
      - schema-ast
    config:
      commentDescriptions: true

  ./src/graphql/types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers
