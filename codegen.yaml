schema:
  - "./src/**/*.(gql|graphql)"
generates:
  ./schema-compiled.graphql:
    plugins:
      - schema-ast
    config:
      commentDescriptions: true

  ./src/types.ts:
    config:
      useIndexSignature: true
    plugins:
      - typescript
      - typescript-resolvers
